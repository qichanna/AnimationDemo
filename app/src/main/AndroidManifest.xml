<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="animationdemo.liqi.com.animationdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BerzierActivity">
        </activity>
        <activity android:name=".MorphingBerzierActivity">
        </activity>
        <activity android:name=".PadBerzierActivity">
        </activity>
        <activity android:name=".WaveBerzierActivity">
        </activity>
        <activity android:name=".PathBerzierActivity">
        </activity>
        <activity android:name=".LoadingActivity">
        </activity>
        <activity android:name=".PathPosTanActivity">
        </activity>
    </application>

</manifest>